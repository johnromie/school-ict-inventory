services:
  - type: web
    name: school-ict-inventory
    runtime: docker
    plan: free
    healthCheckPath: /index.html
    envVars:
      - key: SQLITE_PATH
        value: /var/data/inventory.sqlite
    disk:
      name: school-inventory-data
      mountPath: /var/data
      sizeGB: 1
